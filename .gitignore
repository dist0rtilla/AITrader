predictions_engine/__pycache__/
predictions_engine/.venv/
predictions_engine/.mypy_cache/
__pycache__/
*.py[cod]
*.egg-info/
venv/
.vscode/
config/.env

# Environment variables
.env
.env.local
.env.*.local

# Generated Python/Rust/C artifacts
*.so
*.dylib
*.dll
*.pyd
*.whl

# Model and large binary artifacts
models/**/build/
models/**/dist/
models/**/*.onnx
models/**/*.pt
models/**/*.bin
models/**/*.tar.gz
pattern_engine/target/
pattern_engine/pyo3_wrapper/target/
pattern_engine/**/target/
pattern_engine/*.trt
pattern_engine/*.engine
pattern_engine/onnx/*.onnx

# Frontend/node artifacts
frontend/node_modules/
frontend/dist/

# Python virtualenvs and caches
.venv/
.mypy_cache/
.pytest_cache/

# Temporary backups created during maintenance
/tmp/node_modules_backup_*
/tmp/frontend_dist_backup_*
/tmp/venv_backup_*

# OS and editor files
.DS_Store
Thumbs.db
*.swp
*.swo

# Logs and local temp
*.log
tmp/

# Tensorrt / Tensorflow / ONNX artifacts
*.trt
*.engine
*.plan
*.onnx

# Build artifacts
build/
dist/
out/
*.egg

# Docker build cache (already handled by docker, but exclude local exported tarballs)
*.tar

# IDE / local dev
.idea/
/.local/

# Common repo-level files and local artifacts to ignore
dev.db
docker-compose.yml
docker-compose.*.yml
Dockerfile.backend
Dockerfile.frontend
Makefile

# Node modules at repo root
node_modules/

# Ignore any nested node_modules directories anywhere
**/node_modules/

# Frontend build and public assets
frontend/public/
frontend/dist/
frontend/frontend/dist/

# Project data and generated datasets
data/
market_data/

# Local design and docs drafts
design/

# Execution gateway simulator and local tools
execution_gateway/

# Local model and build directories that shouldn't be committed
models/
predictions_engine/

# Ensure pattern_engine build outputs are ignored
pattern_engine/target/
pattern_engine/native/

# Mock and local servers
mock_mcp/

# Node package lockfiles and optional local artifacts
package-lock.json
yarn.lock


# Frontend local config / sources (some duplicated dirs already ignored above)
frontend/frontend/vite.config.js
frontend/src/main.jsx

# Mock server and local test servers
mock_mcp/server.py

# Backends and build caches
backend/config.json
backend/*.pyc
backend/__pycache__/

# pattern_engine build artifacts (already some are present but be explicit)
pattern_engine/native/
pattern_engine/target/

